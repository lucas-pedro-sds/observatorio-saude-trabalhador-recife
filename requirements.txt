pandas>=2.2
numpy>=1.26
requests>=2.31
SQLAlchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
jupyter>=1.0
matplotlib>=3.8
seaborn>=0.13
openpyxl>=3.1
xlrd>=2.0.1
unidecode>=1.3
basedosdados>=2.0
pysus>=2.11.2
# dbfread/pyreaddbc já vêm como dependência do pysus, mas listamos aqui porque
# pipeline/01_coleta.py os importa diretamente (coletar_sinan_acgr/acbi) — não
# queremos depender de um detalhe de implementação do pysus que pode mudar.
dbfread>=2.0.7
pyreaddbc>=2.0.4
# pysus declara loguru<0.7, mas isso conflita com basedosdados (exige >=0.7). Testado:
# loguru>=0.7 funciona bem com os dois pacotes, apesar do aviso do pip ao instalar.
loguru>=0.7.0