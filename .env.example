# Copie este arquivo para .env (nunca commitar o .env de verdade) e preencha com
# suas próprias credenciais.

# Google Cloud — usado pelo pipeline/01_coleta.py para consultar RAIS/CAGED via basedosdados
GCP_BILLING_PROJECT_ID=seu-projeto-gcp

# PostgreSQL (Neon) — usado pelo pipeline/03_carga.py
DB_HOST=
DB_PORT=5432
DB_NAME=visat_recife
DB_USER=
DB_PASSWORD=
